package chapter3;

import java.util.function.Predicate;

/**
 * Created by simjunbo on 2018-02-17.
 */
public class PredicateTest {
    public static void main(String[] args) {
        /*
        // negate 빨간 사과가 아니다.
        Predicate<Sorting.Apple> notRedApple = redApple.negate();

        // and 빨간 사과이면서 무게가 150이상이다.
        Predicate<Sorting.Apple> redAndHeavyApple = redApple.and(a -> a.getWeight() > 150);

        // or 빨간 사과이면서 무게가 150이상 또는 그냥 녹색 사과
        Predicate<Sorting.Apple> redAndHeavyAppleOrGree = redApple.and(a -> a.getWeight() > 150)
                                                                    .or(a -> "green".equals(a.getColor()));
                                                                    */
    }
}
